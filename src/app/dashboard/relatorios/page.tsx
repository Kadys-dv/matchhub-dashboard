import {Activity,CheckCircle2,Trophy,UserCheck,UsersRound} from "lucide-react";
import {fetchReportSummary} from "@/lib/api";
import {getAccessToken} from "@/lib/session";
import {StatCard} from "@/components/stat-card";
export default async function ReportsPage(){const token=await getAccessToken();let report=null;try{report=await fetchReportSummary(token!)}catch{}if(!report)return <div className="rounded-2xl border border-amber-200 bg-amber-50 p-8 text-center"><h1 className="text-xl font-bold">Relatórios administrativos</h1><p className="mt-2 text-sm text-amber-800">Este módulo exige uma conta ADMIN e a versão atualizada da API.</p></div>;const completion=report.openMatches+report.fullMatches+report.completedMatches?Math.round(report.completedMatches/(report.openMatches+report.fullMatches+report.completedMatches)*100):0;return <div className="mx-auto max-w-[1440px]"><p className="text-sm font-semibold text-emerald-700">Inteligência operacional</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Relatórios</h1><p className="mt-2 text-sm text-slate-500">Resumo consolidado diretamente do banco de dados.</p><section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Usuários cadastrados" value={String(report.totalUsers)} helper={`${report.activeUsers} contas ativas`} icon={UsersRound}/><StatCard label="Participações" value={String(report.totalParticipations)} helper="Confirmações acumuladas" icon={UserCheck} tone="blue"/><StatCard label="Jogos concluídos" value={String(report.completedMatches)} helper={`${completion}% do total`} icon={CheckCircle2} tone="violet"/><StatCard label="Em andamento" value={String(report.openMatches+report.fullMatches)} helper={`${report.fullMatches} partidas lotadas`} icon={Trophy} tone="amber"/></section><section className="mt-7 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-emerald-100 text-emerald-700"><Activity/></span><div><h2 className="font-bold">Saúde da operação</h2><p className="text-sm text-slate-500">Dados calculados em tempo real pela MatchHub API.</p></div></div><div className="mt-6 h-3 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-gradient-to-r from-emerald-500 to-teal-400" style={{width:`${completion}%`}}/></div><p className="mt-2 text-right text-xs font-semibold text-slate-500">{completion}% de conclusão</p></section></div>}
